# Configuration file for Pulp's Resource Manager

# This template defines where the worker's log will be written. %n is filled in with the name of the
# worker.
CELERYD_LOG_FILE="/var/log/pulp/%n.log"

# Configure the log level for the Celery worker here. DEBUG, INFO, WARNING, ERROR, CRITICAL, and
# FATAL are the allowed values.
CELERYD_LOG_LEVEL="INFO"

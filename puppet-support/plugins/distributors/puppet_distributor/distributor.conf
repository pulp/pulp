# Configuration options across all puppet distributor instances

{

    # Directory on disk where the HTTP location is aliased
    "http_dir" : "/var/www/pulp_puppet/http/repos",

    # Directory on disk where the HTTP location is aliased
    "https_dir" : "/var/www/pulp_puppet/https/repos"

}